record:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  module:
    qualified: thesis

  use:
    - invenio

  permissions:
    presets: [ 'workflow' ]

  draft: {}

  record-communities:
    permissions:
      presets: [ "workflow"]


profiles:
  - record
  - draft
  - record_communities


settings:
  schema-server: 'local://'
