record:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  module:
    qualified: thesis

  use:
    - invenio

  permissions:
    presets: [ 'community-workflow' ]

  draft: {}


profiles:
  - record
  - draft


settings:
  schema-server: 'local://'
