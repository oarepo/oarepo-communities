record:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  module:
    qualified: thesis

  use:
    - invenio

  permissions:
    presets: [ 'workflow' ]

  draft: {}


profiles:
  - record
  - draft


settings:
  schema-server: 'local://'
